import classes from './index.module.css';

const Comments = ({ inheritedClasses }) => {
	return (
		<section className={`${inheritedClasses}`}>
			<h3>Comments</h3>
		</section>
	);
};

export default Comments;
