import type { NextPage } from 'next';

import MorseCodeConverterToolScreen from '@components/screens/Tools/ConvertersAndTransformers/MorseCodeConverter';

const MorseCodeConverterTool: NextPage<null> = () => (
	<MorseCodeConverterToolScreen />
);

export default MorseCodeConverterTool;
